language: go

sudo: required

services:
    - docker

go:
    - 1.8

before_install:
    - sudo apt-get update
    - sudo apt-get install git-annex
    - go get -v github.com/G-Node/gin-cli
    - docker pull gnode/ginhome
    - ./start-server.sh

script:
    ./create-upload-download-delete.sh
